/**
 * 自定义全局方法 v3.0
 * */
export function globale() {
  const confirm = () => {
    // MessageBox.confirm(param.title, "提示", {
    //   confirmButtonText: "确定",
    //   cancelButtonText: "取消",
    //   type: param.type || "warning",
    //   center: true
    // })
    //     .then(() => {
    //       if (param.fn) {
    //         param.fn();
    //       }
    //       Message({
    //         type: "success",
    //         message: "删除成功!"
    //       });
    //     })
    //     .catch(() => {
    //       Message({
    //         type: "info",
    //         message: "已取消删除"
    //       });
    //     });
    console.log(1111111);
  };
  return {
    confirm
  };
}
